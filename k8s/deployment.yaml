---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: tenant
  namespace: production
  labels:
    app: tenant
spec:
  selector:
    matchLabels:
      app: tenant
  template:
    metadata:
      labels:
        app: tenant
    spec:
      containers:
        - name: tenant
          image: ${CICD_IMAGE}:${CICD_GIT_TAG}
          imagePullPolicy: IfNotPresent

---
apiVersion: v1
kind: Service
metadata:
  name: tenant
  namespace: production
  labels:
    app: tenant
spec:
  selector:
    app: tenant
  ports:
    - port: 3000
      name: tenant-port
      targetPort: 3000
      protocol: TCP
  type: ClusterIP

---
apiVersion: v1
kind: Secret
metadata:
  name: tenant
  namespace: production
  labels:
    app: tenant
stringData:
  MONGO_USER: tenant-service
  MONGO_PASSWORD: 3yItrBH73TZoykyUVHgR
type: Opaque

---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: tenant
  namespace: production
  labels:
    app: tenant
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-production
spec:
  tls:
    - hosts:
        - devopsapp.ngaxavilabs.com
      secretName: devopsapp-ngaxavilabs-com-tls
  rules:
    - hosts: devopsapp.ngaxavilabs.com
      http:
        paths:
          - path: /api/tenant
            backend:
              serviceName: tenant
              servicePort: 3000
