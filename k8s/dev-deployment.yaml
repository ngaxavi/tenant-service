---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: tenant
  namespace: dev
  labels:
    app: tenant
spec:
  selector:
    matchLabels:
      app: tenant
  template:
    metadata:
      labels:
        app: tenant
    spec:
      containers:
        - name: tenant
          image: ${CICD_IMAGE}:${CICD_GIT_BRANCH}-${CICD_GIT_COMMIT}
          imagePullPolicy: IfNotPresent

---
apiVersion: v1
kind: Service
metadata:
  name: tenant
  namespace: dev
  labels:
    app: tenant
spec:
  selector:
    app: tenant
  ports:
    - port: 3000
      name: tenant-port
      targetPort: 3000
      protocol: TCP
  type: ClusterIP

---
apiVersion: v1
kind: Secret
metadata:
  name: tenant
  namespace: dev
  labels:
    app: tenant
stringData:
  MONGO_USER: tenant-service
  MONGO_PASSWORD: MS4fgoH80A4y538OG40a
type: Opaque

---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: tenant
  namespace: dev
  labels:
    app: tenant
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-production
spec:
  tls:
    - hosts:
        - testing.devopsapp.ngaxavilabs.com
      secretName: testing-devopsapp-ngaxavilabs-com-tls
  rules:
    - hosts: testing.devopsapp.ngaxavilabs.com
      http:
        paths:
          - path: /api/tenant
            backend:
              serviceName: tenant
              servicePort: 3000
